#!/usr/bin/make -f
# -*- makefile -*-
# Sample debian/rules that uses debhelper.
# This file was originally written by Joey Hess and Craig Small.
# As a special exception, when this file is copied by dh-make into a
# dh-make output file, you may use that output file without restriction.
# This special exception was added by Craig Small in version 0.37 of dh-make.

# Uncomment this to turn on verbose mode.
#export DH_VERBOSE=1




CFLAGS = -Wall -g
OPTIONS= prefix=/usr xmlbackend=xercesc release=yes with_osc_support=no double=yes

ifneq (,$(findstring noopt,$(DEB_BUILD_OPTIONS)))
	CFLAGS += -O0
else
	CFLAGS += -O2
endif
ifeq (,$(findstring nostrip,$(DEB_BUILD_OPTIONS)))
	INSTALL_PROGRAM += -s
endif

# shared library versions, option 1
version=2.0.5
major=2
# option 2, assuming the library is created as src/.libs/libfoo.so.2.0.5 or so
#version=`ls src/.libs/lib*.so.* | \
# awk '{if (match($$0,/[0-9]+\.[0-9]+\.[0-9]+$$/)) print substr($$0,RSTART)}'`
#major=`ls src/.libs/lib*.so.* | \
# awk '{if (match($$0,/\.so\.[0-9]+$$/)) print substr($$0,RSTART+4)}'`

configure: configure-stamp
configure-stamp:
	dh_testdir
	# Add here commands to configure the package.

	touch configure-stamp


build: build-stamp
build-stamp: configure-stamp 
	dh_testdir

	# Add here commands to compile the package.
	cd $(CURDIR)/scons/libs; scons $(OPTIONS) configure
	cd $(CURDIR)/scons/libs; scons $(OPTIONS) core processing audioio vmfl vmqt
	cd $(CURDIR)
	#$(MAKE) -C $(CURDIR)/scons/libs build-core
	#$(MAKE) -C $(CURDIR)/scons/libs build-processing
	#$(MAKE) -C $(CURDIR)/scons/libs build-audioio
	#$(MAKE) -C $(CURDIR)/scons/libs build-vmfl
	#$(MAKE) -C $(CURDIR)/scons/libs build-vmqt
	#cd $(CURDIR)/scons/libs && scons $(OPTIONS) core
	# First, tarballize the examples folder
	tar zcvf examples.tar.gz examples
	# and now doxygenate CLAM stuff
	#	doxygen $(CURDIR)/doxygen.cfg
	# tarballize dox
	tar zcvf clam-doxydocs.tar.gz doxygen

	touch build-stamp

clean:
	dh_testdir
	dh_testroot
	rm -f build-stamp configure-stamp

	# Add here commands to clean up after the build process.
	rm -rf $(CURDIR)/scons/libs/*.conf
	-cd $(CURDIR)/scons/libs && scons -c

	find $(CURDIR) -name .*sconf_temp* | xargs rm -rf
	find $(CURDIR) -type f -name *.pyc | xargs rm -rf
	find $(CURDIR) -type f -name *.o | xargs rm -rf
	find $(CURDIR) -type f -name *.os | xargs rm -rf
	find $(CURDIR) -type f -name *.a | xargs rm -rf
	find $(CURDIR) -type f -name .*.swp | xargs rm -rf
	find $(CURDIR) -type f -name .sconsign | xargs rm -rf
	find $(CURDIR) -type f -name *.db | xargs rm -rf
	rm -rf $(CURDIR)/scons/libs/*.conf
	rm -rf $(CURDIR)/debian/libclam-core0/usr
	rm -rf $(CURDIR)/debian/libclam-core-dev/usr
	rm -rf $(CURDIR)/debian/libclam-processing0/usr
	rm -rf $(CURDIR)/debian/libclam-processing-dev/usr
	rm -rf $(CURDIR)/debian/libclam-audioio0/usr
	rm -rf $(CURDIR)/debian/libclam-audioio-dev/usr
	rm -rf $(CURDIR)/debian/libclam-vmfl0/usr
	rm -rf $(CURDIR)/debian/libclam-vmfl-dev/usr
	rm -rf $(CURDIR)/debian/libclam-vmqt0/usr
	rm -rf $(CURDIR)/debian/libclam-vmqt-dev/usr
	rm -rf $(CURDIR)/CLAM.tag
	rm -rf $(CURDIR)/*.tar.gz

	dh_clean 

install: build
	dh_testdir
	dh_testroot
	dh_clean -k 
	dh_installdirs

	# Add here commands to install the package into debian/tmp
	mkdir -p $(CURDIR)/debian/libclam-core0/usr
	mkdir -p $(CURDIR)/debian/libclam-core-dev/usr
	mkdir -p $(CURDIR)/debian/libclam-processing0/usr
	mkdir -p $(CURDIR)/debian/libclam-processing-dev/usr
	mkdir -p $(CURDIR)/debian/libclam-audioio0/usr
	mkdir -p $(CURDIR)/debian/libclam-audioio-dev/usr
	mkdir -p $(CURDIR)/debian/libclam-vmfl0/usr
	mkdir -p $(CURDIR)/debian/libclam-vmfl-dev/usr
	mkdir -p $(CURDIR)/debian/libclam-vmqt0/usr
	mkdir -p $(CURDIR)/debian/libclam-vmqt-dev/usr
	cd $(CURDIR)/scons/libs && scons install_prefix=$(CURDIR)/debian/libclam-core0/usr install_core_runtime
	cd $(CURDIR)/scons/libs && scons install_prefix=$(CURDIR)/debian/libclam-core-dev/usr install_core_dev
	cd $(CURDIR)/scons/libs && scons install_prefix=$(CURDIR)/debian/libclam-core-dev/usr install_sconstools
	rm -rf $(CURDIR)/debian/libclam-core0/usr/lib/*.so
	rm -rf $(CURDIR)/debian/libclam-core-dev/usr/lib/*.so.*
	cd $(CURDIR)/scons/libs && scons install_prefix=$(CURDIR)/debian/libclam-processing0/usr install_processing_runtime
	cd $(CURDIR)/scons/libs && scons install_prefix=$(CURDIR)/debian/libclam-processing-dev/usr install_processing_dev
	rm -rf $(CURDIR)/debian/libclam-processing0/usr/lib/*.so
	rm -rf $(CURDIR)/debian/libclam-processing-dev/usr/lib/*.so.*
	cd $(CURDIR)/scons/libs && scons install_prefix=$(CURDIR)/debian/libclam-audioio0/usr install_audioio_runtime
	cd $(CURDIR)/scons/libs && scons install_prefix=$(CURDIR)/debian/libclam-audioio-dev/usr install_audioio_dev
	rm -rf $(CURDIR)/debian/libclam-audioio0/usr/lib/*.so
	rm -rf $(CURDIR)/debian/libclam-audioio-dev/usr/lib/*.so.*
	cd $(CURDIR)/scons/libs && scons install_prefix=$(CURDIR)/debian/libclam-vmfl0/usr install_vmfl_runtime
	cd $(CURDIR)/scons/libs && scons install_prefix=$(CURDIR)/debian/libclam-vmfl-dev/usr install_vmfl_dev
	rm -rf $(CURDIR)/debian/libclam-vmfl0/usr/lib/*.so
	rm -rf $(CURDIR)/debian/libclam-vmfl-dev/usr/lib/*.so.*
	cd $(CURDIR)/scons/libs && scons install_prefix=$(CURDIR)/debian/libclam-vmqt0/usr install_vmqt_runtime
	cd $(CURDIR)/scons/libs && scons install_prefix=$(CURDIR)/debian/libclam-vmqt-dev/usr install_vmqt_dev
	rm -rf $(CURDIR)/debian/libclam-vmqt0/usr/lib/*.so
	rm -rf $(CURDIR)/debian/libclam-vmqt-dev/usr/lib/*.so.*
	find $(CURDIR)/debian -type f -name .sconsign | xargs rm -rf
	# and copy it to the libclam-doc folder
	mkdir -p $(CURDIR)/debian/libclam-doc/usr/share/doc/libclam-doc
	cp clam-doxydocs.tar.gz $(CURDIR)/debian/libclam-doc/usr/share/doc/libclam-doc
	# and copy it to the libclam-doc folder
	cp examples.tar.gz $(CURDIR)/debian/libclam-doc/usr/share/doc/libclam-doc


# Build architecture-independent files here.
binary-indep: build install
# We have nothing to do by default.

# Build architecture-dependent files here.
binary-arch: build install
	dh_testdir
	dh_testroot
	dh_installchangelogs ChangeLog.txt
	dh_installdocs
	dh_installexamples
#	dh_install
#	dh_installmenu
#	dh_installdebconf	
#	dh_installlogrotate
#	dh_installemacsen
#	dh_installpam
#	dh_installmime
#	dh_installinit
#	dh_installcron
#	dh_installinfo
	dh_installman
	dh_link
	dh_strip
	dh_compress
	dh_fixperms
#	dh_perl
#	dh_python
	dh_makeshlibs
	dh_installdeb
	dh_shlibdeps -L libclam-core0 -l debian/libclam-core0/usr/lib
	dh_shlibdeps -L libclam-processing0 -l debian/libclam-processing0/usr/lib
	dh_shlibdeps -L libclam-audioio0 -l debian/libclam-audioio0/usr/lib
	dh_shlibdeps -L libclam-vmfl0 -l debian/libclam-vmfl0/usr/lib
	dh_shlibdeps -L libclam-vmqt0 -l debian/libclam-vmqt0/usr/lib
	dh_gencontrol
	dh_md5sums
	dh_builddeb

binary: binary-indep binary-arch
.PHONY: build clean binary-indep binary-arch binary install configure
