This directory contains the perl script and the .acv (.ac with perl variables)
files it uses to generate the configure.ac script.

This is used by CLAM-development to generate the configure.ac file and 
should probably not be touched or looked at.
