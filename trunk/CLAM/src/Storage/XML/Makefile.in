TOP=../../..

MODULES = Storage-XML

.DOMPrint.d: DOMPrint.cpp
	@echo Generating $< dependency file \($@\)
	@set -e; $(CC) -M $(CXXFLAGS) $< \
	| sed 's/\($*\)\.o[ :]*/\1.o $@ : /g' > $@; \
	[ -s $@ ] || rm -f $@

include $(TOP)/unix_tools/make.rules
