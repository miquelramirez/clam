This directory contains all (compiled) third party libraries and the needed 
headers to compile CLAM

When making changes to these libraries, they should be located in the same 
directory as CLAM, and should contain install scripts/makefile rules
to copy the compiled library here. 
 
